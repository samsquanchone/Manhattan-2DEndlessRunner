using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BossSpecialAttack : EnemyBullet
{
    /*
    // Start is called before the first frame update
    GameObject enemyAttk;
    
    override protected void Start()
    {
        base.Start();
        float incrementer = 2;

        for (int i = 0; i < 3; i++)
        {
            
            Instantiate(enemyAttk, new Vector3(transform.position.x, transform.position.y + incrementer, transform.rotation);
        }
    }

    // Update is called once per frame
    override protected void Update()
    {
        base.Update();
    }

    */
}
